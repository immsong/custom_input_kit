name: custom_input_kit
description: A customizable Flutter input widget package with Korean/English keyboard, calendar, date/time picker, and number input support.
version: 0.1.0
homepage: https://github.com/immsong/custom_input_kit
repository: https://github.com/immsong/custom_input_kit

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: